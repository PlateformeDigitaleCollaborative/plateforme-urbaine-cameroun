services:
    _defaults:
        autowire: true
        autoconfigure: true

    App\Services\State\:
        resource: '../../../src/Services/State/'

    App\Services\State\Processor\PageView\PageViewLogProcessor:
        arguments:
            $appSecret: '%env(APP_SECRET)%'
